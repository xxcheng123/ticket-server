syntax = "proto3";

package pb;

option go_package="../pb";

message RegisterReq{
  string username = 1;
  string password = 2;
}
message RegisterResp{
  bool ok=1;
  string id=2;
}

service User{
  rpc Register(RegisterReq) returns (RegisterResp);
}
